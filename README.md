# SDN-Orange-Problem
This project demonstrates a functional Software-Defined Networking (SDN) environment using the POX Controller and Mininet. It implements a custom network topology and a reactive controller that manages traffic through OpenFlow match-action rules.
